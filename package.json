{
  "name": "comicadvisorsapp",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "git push heroku master"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0",
    "prerender-node": "^2.5.0"
  }
}
